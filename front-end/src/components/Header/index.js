import React from 'react';
import './header.scss'

const header = props => (
    <header className="navbar">
        <div className="container">
            <h3>MARKETPLACE</h3>
        </div>
    </header>
)

export default header